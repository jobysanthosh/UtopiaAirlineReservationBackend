  
version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 8.10
  install:
    commands:
    - npm install --silent --no-progress -g npm
    - npm install --silent --no-progress -g serverless
  pre_build:
    commands:
    - npm install --silent --no-progress
  build:
    commands:
    - npm test
    - mkdir -p target/stg
    - echo making a target folder
    - mkdir target/prod
    - echo serverless for target stg
    - serverless package --package target/stg --stage stg -v -r us-east-2
    - echo serverless for prod
    - serverless package --package target/prod --stage prod -v -r us-east-2
artifacts:
  files:
  - target/**/*
  - serverless.yml
  - deploy.sh
  